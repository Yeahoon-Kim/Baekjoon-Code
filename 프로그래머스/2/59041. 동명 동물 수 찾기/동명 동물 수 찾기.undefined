-- 코드를 입력하세요
SELECT A.NAME, COUNT(DISTINCT A.ANIMAL_ID) AS COUNT FROM ANIMAL_INS AS A, ANIMAL_INS AS B WHERE A.ANIMAL_ID <> B.ANIMAL_ID AND A.NAME = B.NAME AND A.NAME IS NOT NULL AND B.NAME IS NOT NULL GROUP BY A.NAME ORDER BY A.NAME;